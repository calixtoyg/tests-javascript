/**
 * Should return the last element of the array that is passed as an argument, it should not modify the array.
 */
function getLastElement(arr) {
}

function getFirstElement(arr) {

}

/**
 * Should return number in the middle of the array. If array is not odd it should throw an error
 * @param arr
 */
function getMiddleNumber(arr) {

}

/**
 * Should return the biggest number in the array.
 * @param arr
 */
function getBiggestNumber(arr) {

}

/**
 * Should return the lowest number in the array
 * @param arr
 */
function getLowestNumber(arr) {

}

/**
 * Should return an order array you pass in the arr and asc or desc
 * @param arr of numbers
 * @param order asc or desc
 */
function order(arr, order) {
}

/**
 * Should return a new array without the duplicate numbers
 * @param arr
 */
function removeDuplicates(arr) {

}


export {getLastElement, getFirstElement, getMiddleNumber, getBiggestNumber, getLowestNumber, removeDuplicates, order}
